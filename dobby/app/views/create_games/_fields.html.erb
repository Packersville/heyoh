
<%= f.select :away_team_id, options_from_collection_for_select(@teams, :id, :name, game.away_team_id), {:include_blank => 'Please Select'}%> vs.

<%= f.select :home_team_id, options_from_collection_for_select(@teams, :id, :name, game.home_team_id), {:include_blank => 'Please Select'}%>

<%= f.text_field :date, :class => 'datepicker' %>

<%= f.text_field :time, :class => 'timepicker' %>

<%= f.text_field :spread, :class => 'spread_field' %>

